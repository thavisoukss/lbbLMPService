# LMPS Service — API Reference

**Base URL:** `http://<host>:8084/lmps`  
**Authentication:** All endpoints require `Authorization: Bearer <JWT>` (RS256) except `/auth/**`

---

## Endpoints

### GET /help-check
Health check — confirms the service is running.

**Response**
```
******* LMPS Service working *******
```

---

### GET /get-member-list
Returns the list of PromptPay/QR member banks available for cross-bank transfer.

**Headers**

| Header | Required | Description |
|--------|----------|-------------|
| `Authorization` | Yes | `Bearer <JWT>` |

**No request body.**

**Response `200 OK`**
```json
{
  "status": "success",
  "data": [
    {
      "memberId": "LBB",
      "memberName": "Lao Bullion Bank",
      "applicationId": "LBB_APP",
      "iin": "000001",
      "bankNameLa": "ທະນາຄານ ລາວ ບູລິຍ໋ອງ",
      "bankNameEn": "Lao Bullion Bank",
      "ftModules": "PROMPTPAY",
      "status": "ACTIVE",
      "imageUrl": "https://example.com/lbb.jpg"
    }
  ]
}
```

**Response fields — `data[]`**

| Field | Type | Description |
|-------|------|-------------|
| `memberId` | string | Short bank code |
| `memberName` | string | Full bank name |
| `applicationId` | string | Application identifier |
| `iin` | string | IIN (mapped from `memberIin`) |
| `bankNameLa` | string | Bank name in Lao |
| `bankNameEn` | string | Bank name in English |
| `ftModules` | string | Supported transfer modules |
| `status` | string | Member status (`ACTIVE` / `INACTIVE`) |
| `imageUrl` | string | Bank logo URL |

---

### POST /inquiry-out _(planned)_
Inquiry for an outbound cross-bank transfer — validates recipient and returns fee schedule.

**Headers**

| Header | Required | Description |
|--------|----------|-------------|
| `Authorization` | Yes | `Bearer <JWT>` |
| `Content-Type` | Yes | `application/json` |
| `Device-ID` | Yes | Unique device identifier |

**Request body**
```json
{
  "to_account": "0100001504487",
  "to_member": "IB"
}
```

| Field | Type | Description |
|-------|------|-------------|
| `to_account` | string | Recipient account number |
| `to_member` | string | Recipient member bank code |

**Response `200 OK`**
```json
{
  "status": "success",
  "data": {
    "txnAmount": null,
    "txnId": "INOL2026040308374416KORQNO",
    "frommember": "LBB",
    "fromuser": "PITI-PHANTHASOMBATH",
    "fromaccount": "1200182110002250",
    "toType": "ACCOUNT",
    "toaccount": "0100001504487",
    "tomember": "IB",
    "tomembername": "IB",
    "tomemberimage": "https://example.com/ib.jpg",
    "reference": "LBB4275C3ERT0WDOG8A4ZTM",
    "accountname": "MR PXXXXXXXXX PXXXXXXXXX",
    "accountccy": "LAK",
    "feelist": {
      "LAK": [
        { "from": 0,         "feeamount": 1000  },
        { "from": 2000001,   "feeamount": 1500  },
        { "from": 3000001,   "feeamount": 2500  },
        { "from": 4000001,   "feeamount": 3000  },
        { "from": 5000001,   "feeamount": 4500  },
        { "from": 7000001,   "feeamount": 7500  },
        { "from": 10000001,  "feeamount": 12000 },
        { "from": 30000001,  "feeamount": 15500 },
        { "from": 50000001,  "feeamount": 20000 },
        { "from": 100000001, "feeamount": 25000 },
        { "from": 120000001, "feeamount": 30000 },
        { "from": 150000001, "feeamount": 40000 },
        { "from": 200000001, "feeamount": 50000 }
      ],
      "THB": null,
      "USD": null
    }
  },
  "x_nonce": "b47f8120-d614-4cb5-96fb-bfdc0116f1f1",
  "questions": [
    { "id": "SQ1", "description": "ວັນເກີດຂອງເເມ່ທ່ານເເມ່ນຫັຍງ?" },
    { "id": "SQ2", "description": "ວັນເກີດຂອງພໍ່ທ່ານເເມ່ນຫັຍງ?" },
    { "id": "SQ3", "description": "ເບີໂທເເມ່ທ່ານເເມ່ນຫັຍງ?" }
  ]
}
```

**Response fields**

| Field | Type | Description |
|-------|------|-------------|
| `data.txnId` | string | Transaction ID generated by m-smart |
| `data.frommember` | string | Sender bank code |
| `data.fromuser` | string | Sender user name |
| `data.fromaccount` | string | Sender account number |
| `data.toType` | string | Recipient type (`ACCOUNT` / `PROMPTPAY`) |
| `data.toaccount` | string | Recipient account number |
| `data.tomember` | string | Recipient bank code |
| `data.tomembername` | string | Recipient bank name |
| `data.tomemberimage` | string | Recipient bank logo URL |
| `data.reference` | string | Reference string for the transfer step |
| `data.accountname` | string | Recipient masked account name |
| `data.accountccy` | string | Recipient account currency |
| `data.feelist` | object | Fee tiers per currency (`LAK`, `THB`, `USD`) |
| `data.feelist.LAK[].from` | number | Lower bound of amount tier (inclusive) |
| `data.feelist.LAK[].feeamount` | number | Fee in LAK for this tier |
| `x_nonce` | string | Nonce for the subsequent transfer request |
| `questions` | array | Security questions the user must answer before transfer |
| `questions[].id` | string | Question ID (`SQ1`–`SQ3`) |
| `questions[].description` | string | Question text in Lao |